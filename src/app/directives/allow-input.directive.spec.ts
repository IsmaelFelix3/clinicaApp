import { AllowInputDirective } from './allow-input.directive';

describe('AllowInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowInputDirective();
    expect(directive).toBeTruthy();
  });
});
