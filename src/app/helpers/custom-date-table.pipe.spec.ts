import { CustomDateTablePipe } from './custom-date-table.pipe';

describe('CustomDateTablePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateTablePipe();
    expect(pipe).toBeTruthy();
  });
});
